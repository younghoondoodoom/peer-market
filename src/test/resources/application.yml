spring :
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      enabled: true
      location: /Users/choiyounghoon/Desktop/study/peer-market/src/main/resources/static/files/img
      max-file-size: 128KB
      max-request-size: 128KB

logging.level:
  org.hibernate.SQL: debug