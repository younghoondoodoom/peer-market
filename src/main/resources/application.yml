spring :
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/p2p
    username: sa
    password:

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug